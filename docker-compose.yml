

services:
  web:
    image: kanakjr-website
    # If you prefer to always build from source instead of using the prebuilt image,
    # comment the "image" line above and uncomment the "build" section below.
    # build:
    #   context: .
    #   dockerfile: Dockerfile
    ports:
      - "3000:3000"
    restart: unless-stopped
